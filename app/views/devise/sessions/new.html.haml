.row.justify-content-center
  .col-md-6.align-self-center.pt-5
    %h2.border-bottom.py-2 ログイン

    = simple_form_for(resource, as: resource_name, url: user_session_path, wrapper: :horizontal_long_label_form) do |f|
      .form-inputs
        = f.input :email, required: false, autofocus: true, input_html: { autocomplete: "email" }
        = f.input :password, required: false, input_html: { autocomplete: "current-password" }
        = link_to 'パスワードを忘れた場合', new_password_path(resource_name)
      .form-actions
        = f.button :submit, "ログイン", class: 'btn-success btn-block'
      .sessions_guide
        さくらマーケットのアカウントを持っていない場合は
        = link_to '新規登録', new_registration_path(resource_name)
