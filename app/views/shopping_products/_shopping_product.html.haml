.media.mb-3
  %a.mr-3{href:'#'}
    - product = shopping_product.product
    = image_tag product.image.to_s, size: '100x100'
  .media-body
    %h5.mt-0
      = product.name
    %div
      = "#{product.price}円"
    %div
      = "#{shopping_product.number}個"
    - if block_given?
      = yield
